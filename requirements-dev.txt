# HeritageGuardian AI — Local development extras (NOT for Vercel deployment)
# Install with: pip install -r requirements.txt -r requirements-dev.txt

-r requirements.txt

# Local ASGI server
uvicorn[standard]==0.30.1

# Database migrations
alembic==1.13.1

# Report generation (requires C extension — local only)
reportlab==4.2.0

# Image handling (requires C extension — local only)
pillow==10.3.0

# Testing
pytest==8.2.0
pytest-asyncio==0.23.7

# Code quality
black==24.4.2
isort==5.13.2
